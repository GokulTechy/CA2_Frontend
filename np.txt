Each Activity contains: 
activity id
name
steps
caloriesBurned
workoutMinutes
goalAchieved
date

Dataset contains inconsistent and invalid entries
students must handle them 

application req:
you must build react appl using 
readcontext 
use reducer
React Router

Mandatory rule
/c=activites
/activities/:id/filter
/stats

Mandatory rules
all data must be stored in context(single Source of Truth)
all updatesmust go through Reducer Only
No hardcoding of Dataset
No storing derived val in state
must use map filter reducer

test req:

1.Use test ID:
data-testid= "total-activities"
data-testid= "goal-achieved"
data-testid= "goal- not achieved"
data-testid= "activity - item"

2. Expose computer Value:
window.appState ={
    totalActivities,
    goalAchievedCount,
    goalNotAchievedCount
};

Q1) Display Valid Activities:
Route:/activities

Req:
Display only valid activities
Each activity must be shown using a reusable component

Valid Activity Conditions:L
steps > 0
caloriesBurned > 0
WorkoutMinutes > 0
Goal Achieved must be boolean

Constrains:
Must use.filter()
Must use Context data
Must not mutate Dataset

Edge Cases:
Missing name-> display"Unknown"
Missing date-> display" No Date"

2) Activity detail View
Route:/activites/:id

req:
Display full activity details
Calculate efficiency score

Eff formula:
efficiency = caloriesBurned/workoutMinutes

constrains:
Must Compute dynamically
must Validate ID

edge Cases:
Invalid ID->show"Activity not found"
Division by zero -> handle safely

3) Filter activites
Route:/filter

req:
Filter activities where:
steps>=input Value

const:
Msut use.filter()
Must be dynamic(USerInput)
miust not refetch data

Validation:
Empty-> show error
Invalid Input-> show message

interdependency :
must filter only valid activities